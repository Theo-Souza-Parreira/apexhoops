import estilos from './Footer.module.css'

function Footer(){
    return(
        <footer className={estilos.conteiner}>
            <p className={estilos.titulo}>Footer</p>
        </footer>
    )
}

export default Footer;